%a.btn.btn-default{:type => 'button', 'aria-label' => 'Añadir Cliente', :href => '/customers/new'}
	%span.glyphicon.glyphicon-plus
	Añadir
- @Customers.each do |customer|
	%p
		%h4 #{customer.first_name} #{customer.last_name}
		#{customer.cellphone}
